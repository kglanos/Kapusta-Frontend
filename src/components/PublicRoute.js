// PublicRoute jest komponentem używanym do definiowania tras dostępnych publicznie, czyli takich, które mogą być renderowane, gdy użytkownik nie jest zalogowany. Jest to rodzaj tras, które są dostępne dla wszystkich użytkowników, bez względu na to, czy są zalogowani czy nie.
